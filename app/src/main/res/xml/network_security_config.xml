<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic (your Render https URL) is HTTPS, so cleartext stays OFF by
  default. Cleartext is permitted ONLY for local-development server addresses so
  you can point the app at a server running on your PC / emulator while testing.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>       <!-- emulator -> host PC -->
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
</network-security-config>
